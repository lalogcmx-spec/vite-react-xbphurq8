[variables]
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD = "1"
NODE_ENV = "production"

[phases.install]
cmds = ["npm ci --omit=dev=false"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/app.js"
